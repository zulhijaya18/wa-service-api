// Configuration for the WhatsApp API service
export const config = {
  // Backend URL for Socket.IO connection
  // Leave empty for localhost development
  BACKEND_URL: process.env.BACKEND_URL || ''
};
